import { useState } from "react"

export default function Cart() {

    const [data, setData] = useState(null);
    
    return (
        <div className="cart">
            <h1>hello</h1>
        </div>
    )
}